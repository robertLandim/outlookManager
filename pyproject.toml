[project]
name = "outlookmanager"
version = "0.1.0"
description = ""
authors = [
    {name = "Robert (PC)",email = "landimrobert@gmail.com"}
]
requires-python = ">=3.14"
dependencies = [
    "pywin32 (>=311,<312)",
    "pandas (>=3.0.2,<4.0.0)",
    "openpyxl (>=3.1.5,<4.0.0)",
    "pystray (>=0.19.5,<0.20.0)",
    "python-docx (>=1.2.0,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
